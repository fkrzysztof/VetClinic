@model IEnumerable<VetClinic.Data.Data.Clinic.VisitTreatment>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Visit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Treatment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Visit.VisitID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Treatment.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VisitTreatmentID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VisitTreatmentID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VisitTreatmentID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
